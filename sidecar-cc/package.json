{
  "name": "sidecar-cc",
  "version": "0.0.3",
  "description": "",
  "license": "ISC",
  "author": "weichang.yang",
  "type": "module",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "bun build --compile ./src/index.ts --outfile app-cc  && node ./bin-mv.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": ">=0.2.190",
    "@types/node": "^25.5.0",
    "dotenv": "^17.3.1",
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  },
  "pkg": {
    "assets": [
      "node_modules/**"
    ],
    "scripts": [
      "dist/**/*"
    ],
    "targets": [
      "node24-win-x64"
    ]
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
